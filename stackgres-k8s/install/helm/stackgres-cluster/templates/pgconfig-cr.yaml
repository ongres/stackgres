{{ if .Values.config.create }}
apiVersion: stackgres.io/v1beta1
kind: SGPostgresConfig
metadata:
  name: {{ .Values.cluster.pgconfig }}
  labels:
    app: {{ template "stackgres-cluster.name" . }}
    chart: {{ template "stackgres-cluster.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  {{- if .Values.cluster.create }}
  annotations:
    "helm.sh/hook": "pre-install"
  {{- end }}
spec:
  postgresVersion: "{{ index (.Values.config.postgresql.version | splitList ".") 0 }}"
  {{- with (index .Values.config.postgresql "postgresql.conf") }}
  postgresql.conf:
  {{- range $key, $value := . }}
    {{ $key }}: '{{ $value }}'
  {{- end }}
  {{- end }}
{{ end }}
