top_pg_stat_statements-null-null-null=select pg_database.datname, pg_roles.rolname, pg_stat_statements.* from pg_stat_statements left outer join pg_database on pg_database.oid = pg_stat_statements.dbid left outer join pg_roles on pg_roles.oid = pg_stat_statements.userid
top_pg_stat_statements-null-null-20=select pg_database.datname, pg_roles.rolname, pg_stat_statements.* from pg_stat_statements left outer join pg_database on pg_database.oid = pg_stat_statements.dbid left outer join pg_roles on pg_roles.oid = pg_stat_statements.userid fetch next ? rows only
top_pg_stat_statements-null-asc-null=select pg_database.datname, pg_roles.rolname, pg_stat_statements.* from pg_stat_statements left outer join pg_database on pg_database.oid = pg_stat_statements.dbid left outer join pg_roles on pg_roles.oid = pg_stat_statements.userid
top_pg_stat_statements-null-asc-20=select pg_database.datname, pg_roles.rolname, pg_stat_statements.* from pg_stat_statements left outer join pg_database on pg_database.oid = pg_stat_statements.dbid left outer join pg_roles on pg_roles.oid = pg_stat_statements.userid fetch next ? rows only
top_pg_stat_statements-null-desc-null=select pg_database.datname, pg_roles.rolname, pg_stat_statements.* from pg_stat_statements left outer join pg_database on pg_database.oid = pg_stat_statements.dbid left outer join pg_roles on pg_roles.oid = pg_stat_statements.userid
top_pg_stat_statements-null-desc-20=select pg_database.datname, pg_roles.rolname, pg_stat_statements.* from pg_stat_statements left outer join pg_database on pg_database.oid = pg_stat_statements.dbid left outer join pg_roles on pg_roles.oid = pg_stat_statements.userid fetch next ? rows only
top_pg_stat_statements-test-null-null=select pg_database.datname, pg_roles.rolname, pg_stat_statements.* from pg_stat_statements left outer join pg_database on pg_database.oid = pg_stat_statements.dbid left outer join pg_roles on pg_roles.oid = pg_stat_statements.userid order by test asc
top_pg_stat_statements-test-null-20=select pg_database.datname, pg_roles.rolname, pg_stat_statements.* from pg_stat_statements left outer join pg_database on pg_database.oid = pg_stat_statements.dbid left outer join pg_roles on pg_roles.oid = pg_stat_statements.userid order by test asc fetch next ? rows only
top_pg_stat_statements-test-asc-null=select pg_database.datname, pg_roles.rolname, pg_stat_statements.* from pg_stat_statements left outer join pg_database on pg_database.oid = pg_stat_statements.dbid left outer join pg_roles on pg_roles.oid = pg_stat_statements.userid order by test asc
top_pg_stat_statements-test-asc-20=select pg_database.datname, pg_roles.rolname, pg_stat_statements.* from pg_stat_statements left outer join pg_database on pg_database.oid = pg_stat_statements.dbid left outer join pg_roles on pg_roles.oid = pg_stat_statements.userid order by test asc fetch next ? rows only
top_pg_stat_statements-test-desc-null=select pg_database.datname, pg_roles.rolname, pg_stat_statements.* from pg_stat_statements left outer join pg_database on pg_database.oid = pg_stat_statements.dbid left outer join pg_roles on pg_roles.oid = pg_stat_statements.userid order by test desc
top_pg_stat_statements-test-desc-20=select pg_database.datname, pg_roles.rolname, pg_stat_statements.* from pg_stat_statements left outer join pg_database on pg_database.oid = pg_stat_statements.dbid left outer join pg_roles on pg_roles.oid = pg_stat_statements.userid order by test desc fetch next ? rows only
top_pg_stat_activity-null-null-null=select * from pg_stat_activity
top_pg_stat_activity-null-null-20=select * from pg_stat_activity fetch next ? rows only
top_pg_stat_activity-null-asc-null=select * from pg_stat_activity
top_pg_stat_activity-null-asc-20=select * from pg_stat_activity fetch next ? rows only
top_pg_stat_activity-null-desc-null=select * from pg_stat_activity
top_pg_stat_activity-null-desc-20=select * from pg_stat_activity fetch next ? rows only
top_pg_stat_activity-test-null-null=select * from pg_stat_activity order by test asc
top_pg_stat_activity-test-null-20=select * from pg_stat_activity order by test asc fetch next ? rows only
top_pg_stat_activity-test-asc-null=select * from pg_stat_activity order by test asc
top_pg_stat_activity-test-asc-20=select * from pg_stat_activity order by test asc fetch next ? rows only
top_pg_stat_activity-test-desc-null=select * from pg_stat_activity order by test desc
top_pg_stat_activity-test-desc-20=select * from pg_stat_activity order by test desc fetch next ? rows only
top_pg_locks-null-null-null=select pg_database.datname, pg_locks.* from pg_locks left outer join pg_database on pg_locks.database = pg_database.oid
top_pg_locks-null-null-20=select pg_database.datname, pg_locks.* from pg_locks left outer join pg_database on pg_locks.database = pg_database.oid fetch next ? rows only
top_pg_locks-null-asc-null=select pg_database.datname, pg_locks.* from pg_locks left outer join pg_database on pg_locks.database = pg_database.oid
top_pg_locks-null-asc-20=select pg_database.datname, pg_locks.* from pg_locks left outer join pg_database on pg_locks.database = pg_database.oid fetch next ? rows only
top_pg_locks-null-desc-null=select pg_database.datname, pg_locks.* from pg_locks left outer join pg_database on pg_locks.database = pg_database.oid
top_pg_locks-null-desc-20=select pg_database.datname, pg_locks.* from pg_locks left outer join pg_database on pg_locks.database = pg_database.oid fetch next ? rows only
top_pg_locks-test-null-null=select pg_database.datname, pg_locks.* from pg_locks left outer join pg_database on pg_locks.database = pg_database.oid order by test asc
top_pg_locks-test-null-20=select pg_database.datname, pg_locks.* from pg_locks left outer join pg_database on pg_locks.database = pg_database.oid order by test asc fetch next ? rows only
top_pg_locks-test-asc-null=select pg_database.datname, pg_locks.* from pg_locks left outer join pg_database on pg_locks.database = pg_database.oid order by test asc
top_pg_locks-test-asc-20=select pg_database.datname, pg_locks.* from pg_locks left outer join pg_database on pg_locks.database = pg_database.oid order by test asc fetch next ? rows only
top_pg_locks-test-desc-null=select pg_database.datname, pg_locks.* from pg_locks left outer join pg_database on pg_locks.database = pg_database.oid order by test desc
top_pg_locks-test-desc-20=select pg_database.datname, pg_locks.* from pg_locks left outer join pg_database on pg_locks.database = pg_database.oid order by test desc fetch next ? rows only
