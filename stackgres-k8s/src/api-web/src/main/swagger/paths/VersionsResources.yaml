paths:
  /stackgres/version/postgresql:
    get:
      tags:
      - misc
      summary: List postgres versions
      description: |
        List of the supported postgres versions.

        Allowed values that can be used in the SGCluster postgresVersion definition.

        ### RBAC permissions required

        None
