crdFile: SGPoolingConfig.yaml
type: PoolingConfigDto
schema:
  PoolingConfigDto:
    properties:
      metadata:
        description: |
          Metadata that all persisted resources must have, which includes all objects users must create.
        properties:
          namespace:
            type: string
            description: |
              Namespace defines the space within each name must be unique. An empty namespace is equivalent to the "default" namespace, but "default" is the canonical representation. Not all objects are required to be scoped to a namespace - the value of this field for those objects will be empty. Must be a DNS_LABEL. Cannot be updated. More info: http://kubernetes.io/docs/user-guide/namespaces
          uid:
            type: string
            description: |
              UID is the unique in time and space value for this object. It is typically generated by the server on successful creation of a resource and is not allowed to change on PUT operations. Populated by the system. Read-only. More info: http://kubernetes.io/docs/user-guide/identifiers#uids
      spec:
        properties:
          pgBouncer:
            properties:
              pgbouncer.ini:
                description: |
                  The `pgbouncer.ini` parameters of this given configuration.
  
                  Check [pgbouncer configuration](https://www.pgbouncer.org/config.html) for more information about supported parameters.
      status:
        properties:
          clusters:
            description: |
              List of [SGClusters](https://stackgres.io/doc/latest/04-postgres-cluster-management/01-postgres-clusters/) that are using this configuration.
          pgBoucer:
            description: |
              Connection pooling configuration status based on PgBouncer.
            properties:
              pgbouncer.ini:
                items:
                  description: |
                    The `pgbouncer.ini` parameters of this given configuration. They are represented as an object, where the keys are valid names for the `pgbouncer.ini` configuration file parameters.

                    Check [pgbouncer configuration](https://www.pgbouncer.org/config.html) for more information about supported parameters.

