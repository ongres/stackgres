type: UserPassword
schema:
  UserPassword:
    description: |
      Object that authenticate a user.
    properties:
      username:
        description: |
          Username of the user to be authneticated.
      password:
        description: |
          Password of the user to be authneticated.
