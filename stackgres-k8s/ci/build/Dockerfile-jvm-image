ARG SOURCE_IMAGE_NAME
ARG BASE_IMAGE

FROM "$SOURCE_IMAGE_NAME" as source
  USER root
  RUN mkdir -p /project/
  ARG UID
  RUN chown "$UID:$UID" /project
  USER $UID
  ARG MODULE_PATH
  COPY --chown="$UID:$UID" "./$MODULE_PATH/src/main/docker/" "/project/$MODULE_PATH/src/main/docker/"

