#!/bin/sh

show_logs() {
  find "$(realpath "$TARGET_PATH/logs")" -name '*.log' -type f \
    | grep -v -F 'logs/results.log' | while read log
  do
    echo
    echo ------------------------------------------------------------------
    echo "$log"
    echo ------------------------------------------------------------------
    cat "$log"
    echo ------------------------------------------------------------------
    echo
  done
}